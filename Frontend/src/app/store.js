import { configureStore } from '@reduxjs/toolkit';

// Placeholders for reducers as per project rules
export const store = configureStore({
  reducer: {
    // map: mapReducer,
    // auth: authReducer,
  },
});
